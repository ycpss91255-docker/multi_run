services:
  ci:
    image: bats/bats:latest
    volumes:
      - .:/source
    working_dir: /source
    entrypoint: /bin/bash
    command:
      - -c
      - |
        apk add --no-cache shellcheck
        ./script/ci.sh --ci
